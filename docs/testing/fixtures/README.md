# Fixture Notes

- `source-fixtures.json` contains example source links for seeded local development data.
- `sample-report.pdf` is a tiny checked-in PDF used for deterministic parser tests and CI smoke coverage.
